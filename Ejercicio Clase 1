#Ejercicio 1: un programa que solicita al usuario su nombre y edad, y luego muestra un mensaje de bienvenida, junto con la edad y especificando si esmenor de edad o adult
nombre = input("ingresa tu nombre: ")
Edad = int(input("ingresa tu edad: "))
def mensaje(Edad):
    if Edad < 18:
        return"eres menor de edad"
    else:
        return"eres mayor de edad"

estado = mensaje(Edad)

print(f"Hola, {nombre}, tu edad es {Edad} y {estado}")

#Ejercicio 2: un programa que calcula el área de un círculo a partir del radio proporcionado por el usuario.
radio = float(input("ingrese el radio del circulo a calcular en cm: "))
area = 3.14 * radio ** 2
print(f"El area del circulo es de {area} cm2")

#Ejercicio 3: un programa que calcula el área de un triángulo a partir de la base y la altura proporcionadas por el usuario.
base = float(input("ingrese la base del triangulo a calcular en cm: "))
altura = float(input("ingrese la altura del triangulo a calcular en cm: "))
area = base * altura / 2
print(f"El area del triangulo es de {area} cm2")

#Ejercicio 4: un programa que verifica si un número ingresado por el usuario es par o impar muestra un mensaje correspondiente.
numero = int(input("ingrese un numero entero: "))
def es_par(numero):
    if numero % 2 > 0:
        return "es un numero impar"
    else:
        return "es un numero par"

mensaje = es_par(numero)
print(f"El numero {numero} {mensaje} ")
